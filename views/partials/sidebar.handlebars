<aside id="sidebar" >
    <div class="titleParent">
        <h1>{{name}} </h1>
    </div>
    <div>Total Earnings: <span class="moneyText" id="earnings" >${{totalEarnings}}</span></div>
    <div>Current bank: <span class="moneyText" id="cash" >${{cash}}</span></div>
    <div>Global Rank: {{rank}}</div>
    <div>User since: {{start}}</div>
</aside>
